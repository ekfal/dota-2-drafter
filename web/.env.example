# Salin ke .env.local (jangan commit). FE pakai ANON key (read-only via RLS).
# JANGAN taruh service_role key di sini — itu cuma buat worker.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
